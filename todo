add features:
    --- define new symbols:
        --- variables
        --- functions
    --- perform calls of existing functions;
    --- handle and ouptut errors in input code;
    --- print experssions without calling printf.
